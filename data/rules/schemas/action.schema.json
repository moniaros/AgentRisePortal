{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Action",
  "description": "Defines an action to be executed when a rule's conditions are met.",
  "type": "object",
  "properties": {
    "actionType": {
      "type": "string",
      "enum": ["SEND_EMAIL", "SEND_SMS", "CREATE_TASK", "ASSIGN_LEAD"]
    },
    "target": {
      "type": "string",
      "enum": ["AGENT", "CUSTOMER", "SYSTEM"]
    },
    "template": {
      "type": "string",
      "description": "A template string for the action's content, with placeholders like {policy.policyNumber}."
    },
    "parameters": {
      "type": "object",
      "description": "Key-value pairs for action-specific settings, e.g., { 'assignToAgentId': 'user_123' }.",
      "additionalProperties": true
    }
  },
  "required": ["actionType", "target"]
}